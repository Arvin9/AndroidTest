ext {
    android = [
            compileSdkVersion: 25,
            buildToolsVersion: "25.0.2",
            minSdkVersion: 16,
            targetSdkVersion: 25,
            versionCode: 1,   // 设置版本号
            versionName: "1.0"  // 设置版本名称
    ]
}